---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cf-google-auth
  name: cf-google-auth
spec:
  type: NodePort
  ports:
  - name: "http"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: cf-google-auth

